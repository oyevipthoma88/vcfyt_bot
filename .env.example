# ─────────────────────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values (for local testing only).
# On Heroku, set these as Config Vars in the dashboard / via CLI.
# DO NOT commit your real .env file to git.
# ─────────────────────────────────────────────────────────────────────────────

# ── Required ──────────────────────────────────────────────────────────────────
# Telegram API credentials — https://my.telegram.org
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890

# Bot token from @BotFather
BOT_TOKEN=1234567890:ABCDefGhijKLMnopQRSTuvwxyz

# Your numeric Telegram user ID (get from @userinfobot)
OWNER_ID=987654321

# Log channel ID. Pre-set to -1004303404961 in config.py — override only if needed.
# Bot ko is channel/group mein admin banayein.
LOG_CHANNEL=-1004303404961

# ── Recommended ───────────────────────────────────────────────────────────────
# Pyrogram string session for the userbot (VC account)
# Generate at: https://t.me/Session_generator_1bot  (ya bot mein /login karein)
STRING_SESSION=BQABAgAD...your_session_here...

# MongoDB Atlas URI (free tier — persistent storage across Heroku dyno restarts)
# Get from: https://www.mongodb.com/atlas/database
MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/vcbot?retryWrites=true&w=majority

# ── Optional / Defaults ───────────────────────────────────────────────────────
# Practical playback control scale (0-1000), mapped to real FFmpeg dB gain.
DEFAULT_VOLUME=1000

# Controlled bass lift (0-100). Moderate values preserve clarity.
DEFAULT_BASS=8

# Echo effect on by default (true/false). false is clearer for speech.
DEFAULT_ECHO=false

# Your Heroku app name
HEROKU_APP_NAME=my-vc-audio-bot

# Optional Telegram file_id or public image URL shown by /start.
START_PIC=

# Echo strength 0-10 (only used when echo is enabled)
DEFAULT_ECHO_LEVEL=2

# Loudness stage 0-10
DEFAULT_BOOST=9

# Live mic boost applied automatically to logged-in accounts (1-20000)
LIVE_BOOST_DEFAULT=20000
AUTO_LIVE_BOOST=true

# Optional server/VM or virtual microphone metadata/name for `/mic on`.
MIC_DEVICE=
# Linux usually uses pulse or alsa; set the format supported by your host.
MIC_INPUT_FORMAT=pulse
MIC_DSP=true

# Additional owners, comma or semicolon separated. OWNER_ID remains primary.
OWNER_IDS=123456789,987654321

# Compact VC Audio Relay controls
RELAY_DEFAULT_VOLUME=1000
RELAY_DEFAULT_GAIN=150
RELAY_DEFAULT_BASS=8
RELAY_DEFAULT_TREBLE=62
